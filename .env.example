# ===========================================
# AI Toolkit - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values

# -------------------------------------------
# Database (Required)
# -------------------------------------------
# Neon PostgreSQL connection string
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# -------------------------------------------
# Authentication (Required for Production)
# -------------------------------------------
# Google OAuth credentials
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# NextAuth.js configuration
# Generate secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret"
# NEXTAUTH_URL is auto-detected on Vercel, set manually for other platforms
# NEXTAUTH_URL="https://your-domain.com"

# Base URL for the application (used in OAuth, MCP, sitemap, etc.)
# REQUIRED — no default fallback
NEXT_PUBLIC_BASE_URL="https://your-domain.com"

# -------------------------------------------
# Branding & Site Identity
# -------------------------------------------
# Site name displayed in headers and titles
NEXT_PUBLIC_SITE_NAME="AI Toolkit"

# Contact email for privacy policy and support
CONTACT_EMAIL="admin@example.com"

# Internal organization domain for feature gating
# Users with this email domain see additional tabs (OpenCode, Codex)
# Leave empty to disable internal-only features
INTERNAL_ORGANIZATION_DOMAIN=""

# -------------------------------------------
# AX Dashboard (Optional)
# -------------------------------------------
# Vercel REST API token for the deployed-sites panel
# Create at: https://vercel.com/account/settings/tokens
# Leave empty to show the panel as "not configured"
VERCEL_API_TOKEN=""
# Team scope for the token (personal account: leave empty)
VERCEL_TEAM_ID=""

# -------------------------------------------
# GitHub API (Optional - for Marketplace Sync)
# -------------------------------------------
# Personal access token with 'repo' scope
# Create at: https://github.com/settings/tokens
# Note: Using GH_ prefix because Vercel doesn't allow GITHUB_ prefix
GH_TOKEN="ghp_your_personal_access_token"
GH_OWNER="your-github-org"
GH_REPO="your-repo-name"
GH_BRANCH="main"

# -------------------------------------------
# Gemini API (Optional - for Semantic Search)
# -------------------------------------------
# API key for generating embeddings (gemini-embedding-001)
# Create at: https://aistudio.google.com/apikey
GEMINI_API_KEY="your-gemini-api-key"

# -------------------------------------------
# Analytics (Optional - for analytics Worker)
# -------------------------------------------
# URL of the analytics Worker for event forwarding
# Set in Vercel environment variables for production
# GPTERS_ANALYTICS_URL="https://your-analytics-worker.workers.dev"

# -------------------------------------------
# Development Options
# -------------------------------------------
# Bypass authentication in development (set to "true" to enable)
DEV_BYPASS_AUTH="false"

# Email for dev mock user (used when DEV_BYPASS_AUTH=true)
# DEV_USER_EMAIL="dev@example.com"

# Logging level (debug, info, warn, error)
LOG_LEVEL="debug"

# -------------------------------------------
# Slack Notifications (Optional)
# -------------------------------------------
# Incoming Webhook URL for deploy notifications
# Create at: https://api.slack.com/messaging/webhooks
# SLACK_WEBHOOK_URL="https://hooks.slack.com/services/..."

# -------------------------------------------
# Cron Jobs (Required for Production)
# -------------------------------------------
# Secret token for authenticating cron job requests
# Generate: openssl rand -base64 32
# CRON_SECRET="your-cron-secret"

# -------------------------------------------
# Testing (Optional - for isolated test environment)
# -------------------------------------------
# Separate test database to prevent test data from appearing in production
# Create a separate Neon database branch for testing
# TEST_DATABASE_URL="postgresql://user:password@test-host.neon.tech/testdb?sslmode=require"
