{
  "name": "@gpters-internal/codex-plugin",
  "version": "0.1.2",
  "description": "GPTers Codex CLI Plugin - Agent Skills + MCP 설정 자동 설치",
  "type": "module",
  "bin": {
    "codex-plugin": "./dist/bin/setup.js"
  },
  "files": [
    "dist",
    "skills",
    "templates"
  ],
  "scripts": {
    "build": "bun build src/setup.ts src/paths.ts src/config-toml.ts src/skills-installer.ts src/prompts.ts --outdir dist/src --target node --format esm && bun build bin/setup.ts --outdir dist/bin --target node --format esm && cp -r skills dist/ && cp -r templates dist/",
    "dev": "bun build bin/setup.ts --outdir dist/bin --target node --format esm --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "publishConfig": {
    "registry": "https://verdaccio.gpters.org",
    "access": "restricted"
  },
  "keywords": [
    "codex",
    "cli",
    "plugin",
    "gpters"
  ],
  "author": "GPTers",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gpters/gpters-ai-toolkit.git",
    "directory": "apps/codex-plugin"
  },
  "devDependencies": {
    "@gpters/tsconfig": "workspace:*",
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^3.2.1"
  }
}
