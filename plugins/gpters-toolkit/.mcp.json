{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/schemas/mcp.schema.json",
  "mcpServers": {
    "gpters-marketplace": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/mcp-proxy.mjs"],
      "env": {
        "MCP_URL": "https://company-ai-toolkit.vercel.app/api/mcp",
        "MCP_TOKEN": "${GPTERS_MCP_TOKEN}"
      }
    }
  }
}
